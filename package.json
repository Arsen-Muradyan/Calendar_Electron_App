{
  "name": "calendar",
  "version": "1.0.0",
  "description": "Sample Calendar Desktop App",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-linux": "electron-packager . --overwrite --platform=linux --arch=x64 --icon=src/assets/icon.png --prune=true --out=release-builds"
  },
  "author": "Arsen Muradyan",
  "license": "MIT",
  "dependencies": {
    "electron": "^8.2.4"
  },
  "devDependencies": {
    "electron-packager": "^14.2.1"
  }
}
